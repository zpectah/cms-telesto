import React from 'react';
import { PageLayout, ViewLayout } from '../../components';

const Translations = () => {
  return (
    <PageLayout>
      <ViewLayout breadcrumbs heading={{ title: 'Translations' }}>
        Translations module
      </ViewLayout>
    </PageLayout>
  );
};

export default Translations;
